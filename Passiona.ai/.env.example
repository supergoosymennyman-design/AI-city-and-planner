# Lead-gen destination (spec §10). TODO: point this at a Cloudflare Pages
# Function / Worker that appends leads to a Google Sheet (or forwards to a
# team email). Leave unset to log leads to the server console in dev.
LEAD_FORWARD_URL=
