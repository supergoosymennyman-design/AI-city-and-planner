# dependencies
node_modules/
.pnp
.pnp.js

# build output
dist/
build/
*.tsbuildinfo

# vite
.vite/

# test / coverage
coverage/
playwright-report/
test-results/
.playwright-mcp/

# one-time browser/verification screenshots at repo root (never commit; clean up after use)
/*.png

# logs (work-logs in /logs are committed; these are noise)
*.log
npm-debug.log*

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# env
.env
.env.local
.env.*.local

# self-hosted ML models (large; provenance-pinned & fetched by `npm run vendor-ml`, not committed)
apps/host-standalone/public/ml/
packages/toolbox/assets/
packages/toolbox/models/**/*.bin
packages/toolbox/models/**/*.task
packages/toolbox/models/**/*.wasm

# never commit student/playtest data (R13/§5c)
/playtest-data/

# skill-creator eval workspaces (transient benchmark artifacts)
.ai/skills/*-workspace/
