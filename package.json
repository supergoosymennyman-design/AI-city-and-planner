{
  "name": "ai-education",
  "version": "0.0.0",
  "private": true,
  "description": "AI-Education games platform — kindergarten + primary, React/TS monorepo",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "games/kindergarten/*",
    "games/primary/*"
  ],
  "scripts": {
    "prepare": "node scripts/setup-hooks.mjs",
    "setup": "node scripts/setup-hooks.mjs",
    "typecheck": "tsc --build",
    "contracts": "node scripts/validate-contracts.mjs",
    "sync": "node scripts/sync-agents.mjs",
    "sync:check": "node scripts/sync-agents.mjs --check",
    "vendor-ml": "node scripts/vendor-ml-assets.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "validate": "npm run sync:check && npm run typecheck && npm run contracts && npm run test",
    "build": "npm run typecheck && npm run build --workspace @edu/host-standalone",
    "preview": "npm run preview --workspace @edu/host-standalone",
    "play": "npm run build && npm run preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@testing-library/dom": "^10.4.0",
    "@testing-library/react": "^16.1.0",
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^25.0.1",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.8",
    "zod": "^3.23.0"
  }
}
