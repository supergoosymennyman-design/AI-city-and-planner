# Passiona — AI for Primary 1 & Primary 5

## Run the demo

**Cloudflare demo URL:** https://p5-city-sim.clover-marquis.workers.dev/demo/

Publication is currently pending renewed Cloudflare account authorization; use
localhost below until the deployment is complete. The demo opens the current populated example city automatically, with the conservative
presentation performance settings. The city is labelled **Passiona Demo**.

To run the same demo on localhost, clone the demo branch and run:

```bash
git clone --branch kai-update https://github.com/supergoosymennyman-design/AI-city-and-planner.git
cd AI-city-and-planner
npm install
npm run demo
```

Then open **[localhost:8377/demo/](http://localhost:8377/demo/)**. Use a current
Node.js LTS release and a Bash shell (macOS/Linux, or WSL on Windows). The first
build downloads dependencies; wait for the server's ready message. Later starts
can use `npm run demo:start`. The City demo needs no provider credentials;
Buddy chat requires the separately configured provider. See the
[local demo runbook](P5%20Programme/docs/LOCAL-DEMO.md) for setup and alternate ports.

The demo always selects the existing example rather than a saved empty plan.
Normal student entry remains available at `/hub/`.

## Programme

Passiona teaches AI literacy in Hong Kong primary schools. There are **two
programmes**, each 20 lessons, built on four connected tools — the **AI
Workshop** (build an AI machine), the **Fit Studio** (dress your AI Champion),
the **Coding Buddy** (a bounded agent you direct) and the **AI City** (your 3D
city where built machines are planted):

- **Primary 1 (P1, ages 5–6)** — a playful, hands-on introduction to AI, always
  through touch, sound and pictures, with one clear idea per lesson. P1 lesson
  warm-ups reuse the **kindergarten games** (lesson sources in
  `source/kindergarten/`).
- **Primary 5 (P5, ages 9–10)** — agentic AI literacy: supervised delegation,
  verification, and a finished, evidenced public project.

## Live apps

- **AI City home hub** — "My AI City — Champion Hub", the student entry point:
  https://p5-home.clover-marquis.workers.dev/
- **City sim** (planner + Academy pregame + 3D city on one origin):
  https://p5-city-sim.clover-marquis.workers.dev/

## Get started

Read **`P5 Programme/AGENTS.md`** first — it is the canonical, authoritative
guide to the code: layout, architecture, tests, deploy and verification.

| Path | What it is |
|---|---|
| `P5 Programme/buddy-kit/client/` | Canonical source (3D AI City, planner, hub) |
| `P5 Programme/docs/` | Programme + architecture docs, `.cap` bridge spec |
| `P5 Programme/deploy/` | Build output — regenerated by deploy scripts, never edit |
| `source/kindergarten/` | P1 warm-up game lesson sources (keep) |
| `platform/` | Experimental Unity build (not the browser city) |
| `tests/` | Node unit tests (`npm run test:unit`) |

## Tests

```bash
npm run test:unit      # node:test suite in tests/
npm run test:all       # unit + library audit + Playwright e2e
```

## Notes

- The old top-level "kindergarten/primary game pipeline" (one game per lesson
  across K2/K3 + P1–P6) has been retired; the product is the P1 + P5 Passiona
  programme. Legacy agent definitions may remain under `.opencode/` for
  reference only.
