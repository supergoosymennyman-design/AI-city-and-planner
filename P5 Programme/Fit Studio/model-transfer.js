const DB='passiona-city-model-transfer',STORE='drafts',MAX_AGE=1000*60*30;
function db(){return new Promise((resolve,reject)=>{const r=indexedDB.open(DB,1);r.onupgradeneeded=()=>r.result.createObjectStore(STORE,{keyPath:'id'});r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);});}async function req(mode,fn){const d=await db();return new Promise((resolve,reject)=>{const t=d.transaction(STORE,mode),r=fn(t.objectStore(STORE));r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);t.oncomplete=()=>d.close();});}
export async function getDraft(id){const d=await req('readonly',s=>s.get(id));return d&&Date.now()-(d.updatedAt||d.createdAt||0)<MAX_AGE?d:null;}export async function finishDraft(id,result){const d=await getDraft(id);if(!d)return null;const n={...d,result:{...result,completedAt:Date.now()},updatedAt:Date.now()};await req('readwrite',s=>s.put(n));return n;}
